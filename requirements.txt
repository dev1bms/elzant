# Core framework (existing environment: Python 3.14 + Django 6.0)
Django==6.0.6

# Read configuration from .env (env.bool / env.list API used in settings.py)
django-environ==0.12.0

# Serve static files in production (WhiteNoise)
whitenoise==6.9.0

# WSGI server for production (behind a trusted proxy/tunnel — see DEPLOY.md)
gunicorn==26.0.0

# Image handling for photo greeting cards (ImageField + resize/EXIF-strip/thumbnail)
Pillow==12.2.0

# Inline SVG QR codes for invitation links in the family panel (core/qr.py).
# Pure-Python; the panel degrades gracefully if it is ever missing.
qrcode==8.0

# WhatsApp notifications via Twilio (Content Templates + signed status callbacks).
twilio>=9.0
